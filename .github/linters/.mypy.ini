[mypy]
# Basic configuration for mypy
ignore_missing_imports = True

# Specify the package base explicitly
explicit_package_bases = True

# Add additional paths if needed
mypy_path = src  # Replace 'src' with the path to your package base